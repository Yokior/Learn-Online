#微服务配置
spring:
  application:
    name: content-service
#  datasource:
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://127.0.0.1:3306/xuecheng?serverTimezone=UTC&userUnicode=true&useSSL=false&
#    username: root
#    password: root
  profiles:
    active: dev
  ## 日志文件配置路径
#logging:
#  config: classpath:log4j2-dev.xml
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      discovery:
        namespace: dev
        group: xuecheng-plus-project
      config:
        namespace: dev
        group: xuecheng-plus-project
        file-extension: yaml
        refresh-enabled: true
        shared-configs:
          - data-id: feign-${spring.profiles.active}.yaml
            group: xuecheng-plus-common
            refresh: true
